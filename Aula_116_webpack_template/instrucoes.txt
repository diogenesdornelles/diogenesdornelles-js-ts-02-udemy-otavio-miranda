// npm init -y

// npm i --save-dev @babel/preset-env @babel/cli @babel/core babel-loader webpack webpack-cli regenerator-runtime core-js@2

// gitignore

// mesmo que se apague node_modules, npm i baixa todas as dependências em package.json

// criar webpack.config.js

// configurá-lo

// criar pasta src (source) e inserir o index.js

// criar a pasta public (a que o usuário enxerga) e nela assets (com css, javascript, images)

// modificar script em package.json

// executar script: npm run loadwebpack

// alterar sempre o index em source